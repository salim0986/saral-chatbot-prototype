[pytest]
asyncio_mode = auto
testpaths = tests

# Custom markers
markers =
    groq_integration: marks tests that make real Groq API calls (excluded from default run)

# Exclude groq_integration tests from default run.
# Run them explicitly with: pytest -m groq_integration
addopts = -m "not groq_integration"
